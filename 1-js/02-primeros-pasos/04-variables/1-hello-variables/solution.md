En el código abajo, cada linea representa un item en la lista de tareas.

```js run
let admin, nombre; // se pueden declarar dos variables a la vez

nombre = "John";

admin = nombre;

alert( admin ); // "John"
```
